import { kairos } from "viem/chains";

// chain info
// Please replace this with your own chain info if you are using a different chain.
export const chainInfo = {
  chain: kairos,
  chainId: `eip155:${kairos.id}`,
};